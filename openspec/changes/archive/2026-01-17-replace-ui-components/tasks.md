## 1. Preparation
- [x] 1.1 Confirm the copied design files for sidebar, sonner, toaster, and use-toast are the ones we want to apply
- [x] 1.2 Backup the current implementations for reference

## 2. Apply replacements
- [x] 2.1 Replace `src/components/ui/sidebar.tsx` with the design version
- [x] 2.2 Replace `src/components/ui/sonner.tsx` with the design version
- [x] 2.3 Replace `src/components/ui/toaster.tsx` with the design version
- [x] 2.4 Replace `src/components/ui/use-toast.ts` with the design version

## 3. Validation
- [x] 3.1 Run `npm run build` to ensure the replacements compile
- [x] 3.2 Manually sanity-check the pages that depend on these components (sidebars, toasts)
- [x] 3.3 Update this checklist and the spec to reflect completion
